<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing amount of <%= @ingredient.food.name %></h1>

  <%= render "form", ingredient: @ingredient %>

  <%= link_to "Show this ingredient", meal_ingredient_path(@ingredient), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to meal", meal_path(@ingredient.meal), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
